{
  "$schema": "https://schema.railpack.com",
  "provider": "rust",
  "steps": {
    "build": {
      "commands": [
        "cargo build --release --locked -p scope-cache-service && mkdir -p bin && cp target/release/scope-cache-service bin/scope-cache-service",
        "cp LICENSE NOTICE legal/third-party-rust.txt bin/"
      ]
    }
  }
}
