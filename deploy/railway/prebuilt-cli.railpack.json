{
  "$schema": "https://schema.railpack.com",
  "provider": "shell",
  "deploy": {
    "startCommand": "./start.sh",
    "inputs": [
      {
        "local": true,
        "include": ["bin", "dist", "start.sh", ".scope-deployment-*"]
      }
    ]
  }
}
