{
  "$schema": "https://schema.railpack.com",
  "provider": "shell",
  "deploy": {
    "startCommand": "./bin/scope-cli-service",
    "inputs": [
      {
        "local": true,
        "include": ["bin", "dist", ".scope-deployment-*"]
      }
    ]
  }
}
