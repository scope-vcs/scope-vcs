name: Scope checks
on:
  manual: true
  push:
    branches:
      - main
container:
  image: 957143340948.dkr.ecr.us-east-1.amazonaws.com/scope-vcs/production/checks@sha256:49b9de10106e28db1187a3f0d755c0329b45d22ac16705fa901669b6a36355c1
timeout: 60m
caches: []
env:
  CARGO_HOME: /scope/cache/cargo
  CARGO_TARGET_DIR: /workspace/target
  RUSTUP_TOOLCHAIN: 1.98.0-x86_64-unknown-linux-gnu
jobs:
  backend:
    timeout: 45m
    caches:
      - name: cargo
        path: /scope/cache/cargo
        format: v1
        compatibility: &cargo-cache-compatibility
          files:
            - rust-toolchain.toml
          environment:
            - RUSTUP_TOOLCHAIN
            - CARGO_INCREMENTAL
            - CARGO_PROFILE_DEV_DEBUG
            - CARGO_PROFILE_TEST_DEBUG
          source: false
        exact: &root-cargo-cache-exact
          files: &root-cargo-cache-files
            - Cargo.lock
            - Cargo.toml
          environment: []
          source: false
      - name: cargo-target
        path: /scope/cache/cargo-target
        format: v1
        compatibility: *cargo-cache-compatibility
        exact: &root-cargo-target-cache-exact
          files: *root-cargo-cache-files
          environment: []
          source: true
    env:
      CARGO_INCREMENTAL: "0"
      CARGO_PROFILE_DEV_DEBUG: "0"
      CARGO_PROFILE_TEST_DEBUG: "0"
      CARGO_TARGET_DIR: /scope/cache/cargo-target
      SCOPE_TEST_DATABASE_URL: postgres://scope:scope@127.0.0.1:5432/scope_test
    steps:
      - name: Check Rust workspace
        run: |
          service postgresql start
          su postgres -c "psql --set=ON_ERROR_STOP=1 --command=\"CREATE ROLE scope LOGIN CREATEDB PASSWORD 'scope'\""
          su postgres -c "createdb --owner=scope scope_test"
          ./dev/checks/backend with-api
  cli-checks:
    caches:
      - name: cargo
        path: /scope/cache/cargo
        format: v1
        compatibility: *cargo-cache-compatibility
        exact: &cli-cargo-cache-exact
          files: &cli-cargo-cache-files
            - cli/Cargo.lock
            - cli/Cargo.toml
          environment: []
          source: false
      - name: cargo-target
        path: /scope/cache/cargo-target
        format: v1
        compatibility: *cargo-cache-compatibility
        exact: &cli-cargo-target-cache-exact
          files: *cli-cargo-cache-files
          environment: []
          source: true
    env:
      CARGO_INCREMENTAL: "0"
      CARGO_PROFILE_DEV_DEBUG: "0"
      CARGO_PROFILE_TEST_DEBUG: "0"
      CARGO_TARGET_DIR: /scope/cache/cargo-target
    steps:
      - name: Check CLI
        run: ./dev/checks/cli

  cli-e2e:
    caches:
      - name: cargo
        path: /scope/cache/cargo
        format: v1
        compatibility: *cargo-cache-compatibility
        exact: *cli-cargo-cache-exact
      - name: cargo-target
        path: /scope/cache/cargo-target
        format: v1
        compatibility: *cargo-cache-compatibility
        exact: *cli-cargo-target-cache-exact
    env:
      CARGO_INCREMENTAL: "0"
      CARGO_PROFILE_DEV_DEBUG: "0"
      CARGO_PROFILE_TEST_DEBUG: "0"
      CARGO_TARGET_DIR: /scope/cache/cargo-target
    steps:
      - name: Two-actor contribution flow
        run: |
          cargo build --manifest-path cli/Cargo.toml --locked --bin scope
          ./dev/run-ci-local-stack cli env \
            SCOPE_API_URL=http://localhost:8080 \
            ./dev/checks/integration cli

  web-checks:
    timeout: 45m
    caches:
      - name: pnpm-store
        path: /root/.local/share/pnpm/store
        format: v1
        compatibility: &pnpm-cache-compatibility
          files: []
          environment: []
          source: false
        exact: &pnpm-cache-exact
          files:
            - web/package.json
            - web/pnpm-lock.yaml
            - web/pnpm-workspace.yaml
          environment: []
          source: false
    steps:
      - name: Install dependencies
        run: (cd web && pnpm install --frozen-lockfile)
      - name: Check web
        run: |
          git config --global --add safe.directory "$PWD"
          git init --quiet .
          git add --all -- .gitignore web/src
          ./dev/checks/web

  web-smoke:
    timeout: 45m
    caches:
      - name: cargo
        path: /scope/cache/cargo
        format: v1
        compatibility: *cargo-cache-compatibility
        exact: *root-cargo-cache-exact
      - name: cargo-target
        path: /scope/cache/cargo-target
        format: v1
        compatibility: *cargo-cache-compatibility
        exact: *root-cargo-target-cache-exact
      - name: pnpm-store
        path: /root/.local/share/pnpm/store
        format: v1
        compatibility: *pnpm-cache-compatibility
        exact: *pnpm-cache-exact
    env:
      CARGO_INCREMENTAL: "0"
      CARGO_PROFILE_DEV_DEBUG: "0"
      CARGO_PROFILE_TEST_DEBUG: "0"
      CARGO_TARGET_DIR: /scope/cache/cargo-target
    steps:
      - name: Install dependencies
        run: (cd web && pnpm install --frozen-lockfile)
      - name: Browser smoke against seeded local stack
        run: |
          ./dev/run-ci-local-stack web env \
            SCOPE_SMOKE_REPO=dev/public-demo \
            SCOPE_WEB_BASE_URL=http://localhost:3000 \
            ./dev/checks/integration web

  repository-policy:
    steps:
      - name: Check repository policy
        run: |
          git config --global --add safe.directory "$PWD"
          git init --quiet .
          git add --all
          ./dev/checks/policy

  operations:
    steps:
      - name: Check operations
        run: ./dev/checks/ops

  dependency-analyzer:
    caches:
      - name: dependency-analyzer-npm
        path: /root/.npm
        format: v1
        compatibility:
          files: []
          environment: []
          source: false
        exact:
          files:
            - dependency-analyzer/package.json
            - dependency-analyzer/package-lock.json
          environment: []
          source: false
    steps:
      - name: Check dependency analyzer
        run: ./dev/checks/dependency-analyzer
