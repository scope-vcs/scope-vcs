Both experiments reduced staging check time. Across three repeated warm runs, cache freshness reduced median job time from **487s to 333s**, a 31.6% reduction. Adding database templates reduced it to **207s**, 57.5% below baseline. The source-change check rebuilt and executed the new code, and its subsequent exact-cache repeat reused the updated outputs.

Measured September 7, 2026, in a disposable staging Fargate cluster. Each variant used 4 vCPU, 16 GiB RAM, the same pinned SOCI checks image, and the same source commit `c8354f512cf8ecc11ff5835a7414d166ae0a7984`. The variants had separate job cache namespaces. Each ran formatting, the full Rust workspace tests with API test support, local-dev checks, and Clippy. PostgreSQL started fresh inside each job.

Job time runs from attempt creation to completion and includes dispatch, startup, checkout, cache restore, workload, and cache finalization. Workload time is the measured check script. Queue time before the worker claimed a job is excluded. Each variant has three measured samples.

| Variant | Median job time | Observed range | Median workload | Less job time |
| --- | ---: | ---: | ---: | ---: |
| Baseline | 487s | 482 to 489s | 429.7s | n/a |
| Cache freshness | 333s | 325 to 338s | 280.9s | 31.6% |
| Cache freshness + database templates | 207s | 207 to 223s | 150.3s | 57.5% |

The cache baseline received exact cache hits but still recompiled unchanged code. Cargo diagnostics reported source files newer than restored fingerprint metadata near the Unix epoch. The runtime archive discards the original timestamps, while checkout creates newly dated sources. This matches [Cargo's freshness checks](https://doc.rust-lang.org/stable/nightly-rustc/cargo/core/compiler/fingerprint/index.html). The prototype combines a stable `/workspace` path with restoration of output timestamps and hash-verified unchanged-source timestamps; the measured cache result covers both changes together.

Both optimized variants rebuilt **zero compiler artifacts in every warm run**. Median Cargo compile/check preparation across the three Cargo stages fell from **166.69s to 1.95s**. Tests still executed normally.

Database templates reduced median API test time from **172.54s to 71.71s**, and PostgreSQL tests from **74.20s to 45.48s**. The prototype migrates one template per test process and clones an isolated database for each fixture using [PostgreSQL's template support](https://www.postgresql.org/docs/16/sql-createdatabase.html). Migration-specific tests continue to execute the real migrations. Per-store setup, migration, clone, and cleanup metrics are retained in the results file; their sums overlap concurrent work and must not be interpreted as elapsed job time.

The first template attempt failed because asynchronous database deletion accumulated admin connections and exhausted PostgreSQL's connection slots. Cleanup is now limited to four concurrent deletions. The corrected version passed all 313 API tests and both API integration tests locally with a 100-connection limit; sampled test connections peaked at 20. It then passed the complete staging workloads. There were also two image-pull failures for the pinned digest, despite ECR retaining it, and one connection timeout while claiming an attempt from the staging API. These happened before workload execution and required retries. Startup reliability is a separate follow-up. Failed and seed attempts are retained separately and excluded from the warm statistics.

For the changed-source trial, the test literal and its independently supplied workflow expectation changed from `one` to `two` at `130eb16b627d9ff42befe6affe4dcbee557de149`. Every variant restored a compatible target cache, rebuilt, and passed the complete test suite. A stale test binary would have failed that assertion. The next run received exact hits, and both optimized variants again rebuilt zero artifacts. Local cache checks also covered same-size source edits, dependencies, build inputs, build scripts, Rust flags, build environment, and manifests. In this single core-crate edit sample, job times were **518s baseline, 516s cache-only, and 371s combined**. Cache freshness saved little when that widely used Rust crate needed rebuilding; the large cache-only gains above apply to unchanged-source reruns. The changed-source sample establishes correctness and is not a repeated timing estimate.

I would implement both, starting with cache freshness and its correctness checks. The runtime cache code should own output metadata and content validation. Test fixtures should own database templates, isolation, and bounded cleanup. The prototype remains on `experiment/cache-db-staging`; production runtime behavior was not changed. The measured warm source tree matches GitHub commit `1719df6a`, and the changed-source tree matches `da1de403`. The measured benefit does not depend on smaller images or removing cache-download synchronization.

Code review used `timeout 900 /home/adam-blumoff/.agents/skills/autoreview/scripts/autoreview --mode local` after the code changes, with no actionable findings at the default P0 threshold. Formatting, cache correctness checks, local database integration checks, staging workloads, and teardown ownership checks passed. The shared API fixture path generator also received a uniqueness counter after a local integration collision, consistently across all variants.

Detailed measurements and run/task identifiers: [results-2026-09-07.json](results-2026-09-07.json).

Teardown verified: the disposable AWS stack, CI role, dispatcher user/access key, and experiment secrets are absent. All 27 task definitions were deregistered and accepted for deletion; AWS still reports `DELETE_IN_PROGRESS` while it finishes purging them. The 11 experiment cache objects were deleted and their prefix verified empty. The fixture repository was deleted and verified absent, and its CLI session was revoked. Staging API, cache, and worker services are back offline, with the worker's original variables restored. Production services and resources were not changed. [Cleanup evidence](cleanup-2026-09-07.json) includes the successful workflow and verification results.
